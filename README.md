# Run and deploy your AI Studio app

This is a react application that shows a report after analyzing "Account & Transactions" api and the "Commercial Variable Recurring Payments" api.

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
